[build]
  builder = "nixpacks"

[deploy]
  startCommand = "python backend_v3.py"
  healthcheckPath = "/health"
  healthcheckTimeout = 30
  restartPolicyType = "on_failure"
  restartPolicyMaxRetries = 5
